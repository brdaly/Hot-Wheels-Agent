# Agent specification

`HOT_WHEELS_FRONTIER_AGENT.md` is the canonical human-readable operating specification. Runtime code mirrors its score maxima, tier boundaries, identity confidence, ownership, and evidence rules. Changes to the specification require scoring tests and a model-version note.
